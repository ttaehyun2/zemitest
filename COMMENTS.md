# 댓글 기능 설정 안내

댓글은 **Upstash Redis**와 **관리자 토큰**이 모두 있어야 동작합니다.
설정 전에는 댓글 영역이 아예 표시되지 않고, 사이트의 다른 기능은 정상 동작합니다.

---

## 1. 환경변수 설정

Vercel → 프로젝트 → **Settings** → **Environment Variables** 에 추가합니다.

| 이름 | 값 | 설명 |
|---|---|---|
| `UPSTASH_REDIS_REST_URL` | 자동 주입 | Upstash 연결 시 자동 |
| `UPSTASH_REDIS_REST_TOKEN` | 자동 주입 | Upstash 연결 시 자동 |
| `ADMIN_TOKEN` | 직접 정하기 | 댓글 관리자 비밀 문자열 |
| `IP_SALT` | 아무 무작위 문자열 | 도배 방지 해시용 |

**⚠️ `ADMIN_TOKEN`을 설정하지 않으면 관리 기능이 항상 잠깁니다.**
즉 아무도 댓글을 삭제할 수 없게 되니 반드시 설정하세요.

토큰은 길고 추측하기 어려운 문자열로 정하세요. 다른 곳에서 쓰는 비밀번호는
쓰지 마세요.

설정 후 **Redeploy** 해야 반영됩니다.

---

## 2. 관리 화면

```
https://zemitest.com/admin/comments
```

접속하면 토큰 입력창이 뜹니다. 입력하면 전체 댓글이 페이지별로 보입니다.

- **삭제** — 댓글 한 개 지우기
- **작성자 차단** — 그 사람이 쓴 모든 댓글 삭제 + 24시간 차단

토큰은 브라우저 세션에만 잠시 저장되고 탭을 닫으면 사라집니다.
검색엔진에는 노출되지 않습니다 (robots.txt + noindex).

---

## 3. 자동으로 걸러지는 것

**연락처·외부 유도**
- 전화번호 (010-1234-5678 등)
- URL, 도메인 (.com, .kr 등)
- 카톡·디스코드·텔레그램 아이디 요구
- SNS 핸들 (@아이디)

연락처 차단은 스팸 방지이면서, 또래 이용자가 많은 사이트에서 개인 연락처가
오가는 것을 막기 위한 조치이기도 합니다.

**욕설·도배**
- 명백한 욕설과 비방
- 같은 글자 10회 이상 반복
- 기호만 있는 글, 2자 미만

**속도 제한**
- 30초에 1개
- 시간당 10개

---

## 4. 그래도 사람이 봐야 하는 것

필터가 모든 걸 잡지는 못합니다. 아래는 직접 확인해야 합니다.

- 우회 표현을 쓴 비방 (초성, 띄어쓰기 변형 등)
- 특정인을 겨냥한 저격 글
- 맥락상 문제가 되는 내용

**하루 한 번 관리 화면을 확인하는 습관**을 들이는 걸 권합니다.
특히 정치 성향 테스트 페이지는 다툼이 생기기 쉬우니 자주 봐주세요.

방치된 댓글은 애드센스 계정에 문제가 될 수 있습니다. 구글은 사이트에 올라온
이용자 글에 대해서도 운영자에게 책임을 묻습니다.

---

## 5. 저장 구조

| 키 | 내용 |
|---|---|
| `cmt:{페이지ID}` | 댓글 목록 (최근 300개 보관) |
| `cmt:pages` | 댓글이 달린 페이지 목록 |
| `ban:{해시}` | 차단된 작성자 |
| `cd:{해시}`, `hr:{해시}` | 도배 방지 카운터 |

IP 주소는 **원본을 저장하지 않고 해시만** 사용합니다. 닉네임 외에 개인정보는
수집하지 않습니다.
